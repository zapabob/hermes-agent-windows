# T05 pre-change CodeGraph source map

Source snapshot: `9b275d0818e21d2c5c4db0ba998f2833a42013ab` on `codex/t05-parent-inference-20260924`, isolated worktree. The approved CLI is `@colbymchenry/codegraph` 1.6.0. The index at the isolated worktree reports complete extraction v25, 8,903 files, 190,479 nodes, 609,557 edges, zero pending changes, no worktree mismatch, and no reindex recommendation.

The bounded T05 graph pass used three queries: delegation construction and cancellation exploration; AIAgent initialization, credential resolution and normal request transport exploration; and depth-three impact for `_build_child_agent`. The first flow resolves `delegate_task` through `_build_child_preserving_parent_tools` and `_build_child_agent` into `run_agent.AIAgent`; `SubagentLifecycleService.launch` is a separate constructor caller. Direct source inspection found that `_build_child_agent` reads the parent key/client kwargs, resolves a child credential pool, then initializes an `AIAgent`; `init_agent` resolves Anthropic tokens or provider clients when explicit credentials are absent. The normal inference seam is the conversation loop's `_perform_api_call`, which selects interruptible streaming or the ordinary non-streaming helper. The auxiliary `call_llm` path in `agent/auxiliary_client.py` is separate and needs a fail-closed guard in inference-port mode.

The impact result reported 85 affected nodes, including delegated construction, lifecycle launch, nested orchestration, credential-routing tests, fallback tests, and child stop/cancel coverage. This is a source-map summary, not a claim that every dynamic caller was enumerated.

Unresolved dynamic edges for direct checks: auth/profile resolvers reached during child initialization; auxiliary compression or optional-memory calls; child cancellation reaching a request-local client abort; HERMES_HOME ContextVar propagation through lifecycle/delegate worker threads; nested delegation from a port-owned child. Planned direct checks are resolver tripwires at the actual AIAgent constructor, profile A/B/A port calls, cancellation during synthetic transport, an ordinary tool-call round trip, controlled child/grandchild launch, and the existing delegation/lifecycle/auxiliary-client regressions.

The permitted scope was expanded before edits to `agent/conversation_loop.py` and `agent/chat_completion_helpers.py`, because the CodeGraph path reaches the normal request dispatcher and cancellation/client owner there. No other agent owns those files. The ordinary `delegate_task` path remains the compatibility path unless a trusted lifecycle admission context is present.
