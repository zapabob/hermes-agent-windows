# T10 Admission Evidence

The isolated worktree was based on integrator commit `290e5812c6fa0a28306de6b67ce0d5a629284213`. The implementation commit is `fbf81eae67954eca93b3e033f02f55e4ed889bf1` (`feat: add inactive delegation admission owner`). It adds `downstream/delegation/admission.py` and the two assigned test files only.

## Behavior and TDD

The owner serializes route and resource reservations in memory, binds each grant to profile, account, provider scope, route revision, tool scope, parent lineage, and monotonic soft, idle, and hard deadlines, rotates among eligible free routes, and rejects paid or unknown routes. It enforces two top-level reservations and one read-only depth-two grant, including across parent grant IDs. Unknown launch outcomes retain resource reservations and block relaunch until a trusted terminal reconciliation call. It has no child launcher or scheduler.

The first meaningful RED used the unimplemented reservation stub: the assertion `assert isinstance(result, AdmissionGrant)` received `AdmissionRejection(code='not_implemented', retry_after_seconds=None)`. The `admission.py` SHA-256 at that RED was `dee1bb29409a0418164119c51e4490494b3970f9`. After implementation, the test passed. A later lineage regression also produced RED: a nested request changed `provider_scope` and received a grant; the `admission.py` SHA-256 at that RED was `be1a76595e3276ae06116e0eb38ff75343b6e3dd83334e914885a53f4f533027`. The correction binds nested requests to the parent's provider scope, after which the test returns `parent_scope_mismatch`.

Focused verification passed: T10 tests, 12 passed; T08 resource admission regressions, 22 passed; T09 free-route catalogue regressions, 24 passed. Ruff passed on the three owned Python files. The configured `ty check` passed on the same files.

## CodeGraph 1.6.0

The before receipt and queries are in `evidence/codegraph/T10-290e5812-before.json` and `evidence/codegraph/results/T10-290e5812-before-results.json`. The after receipt and queries are in `evidence/codegraph/T10-fbf81eae-after.json` and `evidence/codegraph/results/T10-fbf81eae-after-results.json`. The after sync used Node 22.23.2 and pinned CodeGraph 1.6.0. Status reports 8,918 indexed files, 191,125 nodes, 612,054 edges, zero pending file changes, zero pending references, a complete index, and no worktree mismatch. The indexed source fingerprint is `c19d89f3c9306c7a462f8d79b24700608ab1df31d3507cede0ec6d434eed132d`.

The caller query for `DelegationAdmission.reserve` finds only the T10 test helper. A direct source search also found no production reference to the admission module in `tools/delegate_tool.py`, `agent/subagent_lifecycle.py`, `hermes_cli`, or other `downstream` Python files. CodeGraph's `affected` query returned no tests, and its callee query included unrelated name matches while omitting the direct `ResourceReservationBook.reserve` edge; source inspection confirms the explicit T08 reservation call, and the existing T08/T09 regression suites were run directly.

## Integration boundary

This is an inactive admission owner. No production caller, writer adapter, or lifecycle hook is connected. Activation still depends on trusted T05 lineage and tool scope, T06 isolation, fresh T08 resource observations and reservation ownership, validated T09 catalogue snapshots, and a host lifecycle owner that enforces deadlines and reconciles terminal outcomes. This slice does not prove real child execution, mutation safety, deadline enforcement, or full T10 acceptance.
