name: Temporary regenerate carry metrics

on:
  push:
    branches:
      - fix/test4-parent-incarnation-gate

permissions:
  contents: write

jobs:
  regenerate:
    if: github.actor != 'github-actions[bot]'
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0
          ref: fix/test4-parent-incarnation-gate
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39
        with:
          version: '0.9.28'
      - run: uv python install 3.11
      - run: uv sync --locked --python 3.11 --extra dev
      - name: Regenerate carry metrics
        shell: pwsh
        run: |
          uv run --no-sync python scripts/downstream/carry_metrics.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          uv run --no-sync python scripts/downstream/carry_metrics.py --check
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Commit refreshed metrics
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          if git diff --quiet; then
            echo 'No carry metric changes required.'
            exit 0
          fi
          git add _docs/carry-surface-20260826.json _docs/carry-surface-20260826.md
          git -c user.name='github-actions[bot]' -c user.email='41898282+github-actions[bot]@users.noreply.github.com' \
            commit -m 'docs(downstream): refresh carry metrics for TEST 4 parent gate'
          git push origin HEAD:fix/test4-parent-incarnation-gate
