# notes/ — tracked operator archives

Tracked material that must stay in git but must **not** clutter the repository
root lives here. Ignored scratch still belongs under `output/` and `tmp/`
(see `fork/local-workspace/README.md`).

| Path | Purpose |
|------|---------|
| `archives/mcp-research-data/` | UE bench / discovery JSON (relocated from root `mcp-research-data/`) |
| `archives/net-home-probe-results/` | Network home-probe baselines (relocated from root `results/`) |
| `archives/artifacts/` | Measurement artifacts (relocated from root `artifacts/`) |

Do not put secrets here. Prefer CodeGraph + root `AGENTS.md` §§16–17 before
adding new root-level files.
