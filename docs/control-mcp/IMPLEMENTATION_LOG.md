# Hermes Control MCP implementation ledger

Updated: 2026-09-24 JST. Repository: `zapabob/hermes-agent-windows` only. This ledger records observed results; an unchecked item is not a completed capability.

## Provenance and isolation

- Verified baseline `origin/main`: `34562508555d84f0740eda7a0af5ec4ff90bbe60`. Approved document source: `73e6c5f01b961c25ca422db2a9e923fe56d504c2`.
- Advanced handoff archive had six original commits ending `b87ac9483fd5b18f7d2ad2e7d6be1dce836078ba`; it was replayed into the isolated Windows worktree `hermes-control-mcp-recovered-20260923`, branch `feat/hermes-control-mcp-c-20260923-recovered`. New replay SHAs: `9cd69f18f6ee0c61e0c18f38484844b6e4c66a45`, `bfbc2e124c91c155cae89fcb2f25a127c3d68cda`, `bf544a5e497a3224d050d76957531a1457ee8ed3`, `76e0f45276952afee26c028b968568c5bad4107a`, `117250d0230a7d8adf613afdd909149613059f0f`, `af38acb37e343f1192bfe7190b37fd433fd54044`.
- Approved design and plan commits: `2581f41ed18ba15331ab53c402ca1cb1cb827382`, `deb80b356485d23dd3850253eb3a0547d8b4052c`. The user explicitly authorized implementation, TDD, isolation and conditional merge in the task request; older plan text requesting implementation approval does not override that instruction.
- The original feature worktree and the 0.21.4/upstream worktrees were preserved. The primary checkout and running Desktop/Go/llama processes were not changed.
- During this continuation, `origin/main` advanced to `e6070028c9d0d75634ad7661c33fa937b682474a` through PR #144. Its engineering effort/diagnostic changes were reviewed and merged only into this isolated feature worktree at `15b3289771fce4133a5978d53c127bcd9b54f042`. The sole textual conflict was the native host import; both RunResult evidence and upstream diagnostic imports were kept. No main checkout edit was made.

## Task results

| Task | State | RED and GREEN evidence | Code SHA | Remaining gate |
| --- | --- | --- | --- | --- |
| 0 freeze/baseline | Partial | Source/branch/worktree inventory and locked `uv sync --locked --python 3.11 --extra all --extra dev` completed. Initial sync failed for low C: space, then succeeded after space recovered. | n/a | Full ordinary baseline and CI pending. |
| 1 read facade | Partial | Handoff recorded import-only RED separately from meaningful assertions. On this Windows PC, component suite initially: 130 passed, 1 skipped; no live client read. | `9cd69f18`, `bfbc2e12`, `af38acb3` | Persisted typed evidence is now advertised only by the wired reader; active live-run observation remains false. Real host enablement and client read remain pending. |
| 2 journal/approval | Partial | Handoff journal/decision tests are behavioural. New TUI tests first failed on missing strict RPC and permissive choices, then passed. Desktop tests first failed on normal RPC, missing digest and reconnect mapping, then passed. File-isolated Python run: 217 passed, 1 skipped across five files. Desktop focused tests: 63 passed. TUI focused tests: 120 passed. | `bf544a5e`, `a33bc9d23b8860b274545e65ba5e60a8e92bc676`, `1090d7bb4707ac0a46b540b859c42da6af2fc94a` | A tested host coordinator now reserves once, presents through the existing strict approval queue, waits outside the DB transaction and dispatches only an actual once decision. Ambiguous executor dispatch retains UNKNOWN; timeout releases only an unexecuted intent. It is not yet constructed by the live web host or bound to a selected human session. The real SDK now exercises a conditional start tool and the native owner through a controlled in-process host, but product-host UI approval and live MCP mutation remain unproven. |
| 3 MCP auth/protocol | Partial | SDK import RED, then HTTP 404 behaviour RED before transport wiring. Real locked SDK client, two scoped identities, revocation, parent mount/dashboard bypass: GREEN. | `76e0f452`, `117250d0`, `b355c937c4f384154730fc883a239c32067c0f44`, `1d4919cac1438c2b62a3eabf45e27606ecfa8c3a` | No approved issuer/client registration, startup config, operational endpoint or live client auth. The default/unwired host advertises no write tool; the conditional tested host registers only engineering start. |
| 4 engineering owner/evidence | Partial | Native host writes fsynced manifest, terminal result and typed check receipts, now with a host-issued operation ID. Approved journal claims are atomic and feed one native entrypoint invocation through an injected host executor; generation-bound cancellation rejects stale/foreign IDs. Meaningful RED on missing receipt/unsupported reader, missing run owner API, blocked execution and foreign transport approval, then GREEN. File-isolated owner/approval regression run: 342 passed, 2 skipped across 15 files. After main integration, 20 engineering/control files: 280 passed, 3 skipped. Ruff passed on changed Python files. Earlier 17-file engineering run: 289 passed, 3 skipped. Negative cases cover failed exit, timeout, wrong attempt, missing check, source/route mismatch, foreign transport and cross-workspace denial. | `5e985dbb8e8eeda541851699643e7c0e5a47e1a8`, `d5b2c5ea619c6a78663aae1eb4574ad58c06d0c8`, `03b82b2d5646b0ad0dea99e5595800d98eab2a7c` | Run owner is not yet constructed by the live product host; the conditional MCP start tool uses it in a controlled test; it requires trusted preflight/evidence callbacks. Reverify, Docker acceptance and live producer session remain pending. |
| 5 route CAS | Not started | n/a | n/a | Native picker validation and atomic config authority. |
| 6 verified apply | Not started | n/a | n/a | Provenance and guarded destination promotion. |
| 7 PR/merge controls | Not started | n/a | n/a | Parent-only Git authority, separate approval and remote protection checks. |
| 8 qualification | Not started | n/a | n/a | i18n, 24 negatives, real Windows/Docker, two clients, exact-head CI/security review. |

## Test boundaries and defects

- Direct multi-file pytest in one interpreter reported 5 existing approval failures (unattended/timeout). Those two representative cases passed alone; the repository-prescribed file-isolated runner passed all five files with 217 tests, 1 skip, no retries. The direct run is retained as a test-isolation finding, not concealed.
- Desktop renderer typecheck in the recovered worktree uses junctions to the untouched primary checkout's `node_modules`. It exits 2 with approximately 1,387 duplicate React type errors from the shared dependency path; filtered errors in changed files were 0 after fixing event and resume typings. This does not qualify as a full typecheck pass. TUI typecheck passed. ESLint on changed Desktop/TUI files had no errors; pre-existing padding warnings in the TUI event handler remain.
- The one skipped Python test in the earlier approval run depends on platform privilege; it is not a successful Windows negative scenario. The later engineering component run had 3 skips, reported separately from passes.
- The latest owner tests include one call through the actual `run_workflow` function with a fake native host. This proves identity plumbing, not a live MCP mutation.
- Windows real native Docker E2E: `tests/e2e/test_engineering_native_docker.py` with the already-present local image `sha256:0c593db2a6dcc5d9a39177b6ea2f83a4fd3cfe7289d4502ce093a7f8fa8789b6`: 2 passed in 85.63 seconds before main integration and 2 passed in 122.65 seconds afterward (concurrent regression load). The test uses deterministic local HTTP inference and validates native container/child/grandchild behaviour; it does not establish paid-account entitlement, live MCP approval or external client authentication. No matching engineering container remained running after the test.
- No remote endpoint, tunnel, client account, provider credential, PR or main merge was activated by these tests. No production write is enabled. The coordinator and run owner are host-side components only; neither is mounted as an MCP mutation.

## 2026-09-24 continuation: trusted dispatch boundary

- Commit `40a6f3eda7bfbba20f42d3de58cd693dda7b4389` addresses Task 2/4 admission and native-executor races. An actual `ContextVar` profile RED showed a missing home in the approval waiter; a second RED showed that copying the entire context exported a synthetic secret marker. The host now carries only the resolved Hermes profile home, with no generic context copy. Both tests passed after the change.
- RED for post-consent grant revocation and pre-native-worker revocation failed on absent live grant checks. The host coordinator and run owner now require a trusted `revalidate_grant` callback at admission/decision/start/native preflight. Revoked operations block without native effect. This is a host interface requirement; no production verifier is mounted yet.
- RED for accepted-then-exception executor dispatch exposed a worker/transition race. The worker now waits on an admission event and aborts if scheduling acknowledgement fails. The journal stays UNKNOWN with its workspace reservation, and native execution count remains zero.
- Verification on the above commit: `python -m pytest tests/control_mcp/test_host_dispatch.py tests/control_mcp/test_engineering_owner.py -q` gave 12 passed; `scripts/run_tests_parallel.py -j 2 tests/control_mcp -q` gave 158 passed, 1 skipped across 12 file-isolated files. Ruff on all six changed Python files passed. These are local component gates, not an operational write or security review.

## 2026-09-24 continuation: operation identity isolation

- Commit `57183fdf016039638e9b2429bda97633cc13ba93` closes an operation-read access gap. A new test first showed that another client with the same workspace grant could read a known operation ID. Journal reads now require the original subject, client registration, resource and grant revision. Identical idempotency keys from a later grant revision conflict instead of silently returning an old operation. `tests/control_mcp/test_operations.py`: 16 passed; file-isolated `tests/control_mcp`: 159 passed, 1 skipped across 12 files. Ruff passed.
- Current official ChatGPT Help Center documentation says Pro custom MCP access is read/fetch only, while full MCP is for Business and Enterprise/Edu. This is a current product-documentation result, not an observed account/client test. No write action was relabeled as read.

## 2026-09-24 continuation: real protocol admission

- Commit `5ac8734b57728d7cc6f4f09d539b6287614970a3` adds a conditional `hermes_start_engineering_run` Streamable HTTP tool. The default/unwired service still advertises write=false and does not register it. A host-supplied coordinator exposes only start, with explicit per-operation capability flags and scope-aware write availability.
- Real locked MCP SDK client RED first failed on the missing service coordinator; GREEN exercises initialise/list/call, two authenticated clients, one workspace reservation, bound Hermes `once` decision, one host-owner effect and idempotent retransmission. A second RED showed the SDK silently dropped unknown write fields before invoking the function. The server now checks raw write arguments, so model-supplied `approved=true` is rejected before any approval ticket exists. A read-only client cannot request the mutation even when the host grant later permits more scopes.
- `scripts/run_tests_parallel.py -j 2 tests/control_mcp -q`: 160 passed, 1 skipped across 12 file-isolated files. Changed-file Ruff passed. Commit `3bae13831895820b20d1a27ff74d03e574a5323d` strengthened the protocol test to call the real `EngineeringRunOwner` (focused test 1 passed), with `run_workflow` monkeypatched at the native boundary. This is not live Docker, actual ChatGPT/Codex Desktop acceptance or product-host activation. Task 4 remains partial and Tasks 5–8 remain open.

- Task 4 evidence review commit `a7ccf29e7d6991c21d1c541a42a27141db5142f9`: two successful-looking native check rows for the same attempt but different revisions were incorrectly projected as verified. The new behavioural case was RED, then the reader required one shared revision; `tests/control_mcp/test_evidence_provenance.py` finished 11 passed, Ruff passed. This is receipt consistency, not a claim that provenance alone authorises application.

## 2026-09-24 affected-regression gate

- On feature HEAD `387808de415637580b3d3a4a280d19da07d7a57c`, the file-isolated runner covered the handoff's affected command set: `tests/control_mcp`, `tests/tools/test_approval.py`, `tests/tools/test_approval_interrupt.py`, `tests/tools/test_mcp_elicitation.py`, `tests/hermes_cli/test_dashboard_token_auth.py`, `tests/hermes_cli/test_config.py`, `tests/hermes_cli/test_config_env_expansion.py`, `tests/hermes_cli/test_managed_scope_config.py`, `tests/hermes_cli/test_read_raw_config_readonly.py`, `tests/test_mcp_serve.py`, `tests/implementation_router`. Command: `python scripts/run_tests_parallel.py -j 2 [these paths] -q`. Result: 25 files, 575 passed, 0 failed, 3 skipped, 66.2 seconds. This runner uses subprocess isolation; it does not substitute for the ordinary full suite or CI.

## Current release gates

Do not advertise C or merge while Tasks 4–8, real client authentication/read/approved-write, full exact-head checks and security review remain open. Live endpoint and client settings require a separate concrete operator approval; Pro feature limitations must be reported as client limitations rather than masked as read results.

## 2026-09-24 operator steering: selective retirement and general Hermes MCP

- The operator changed the target architecture after the original Control MCP plan: selectively retire #143/#144's fixed actor/Docker Router, keep generic infrastructure, and provide general Hermes through MCP with a subagent path. `docs/control-mcp/RETIREMENT_SPEC.md` fixes per-file disposition and supersedes the old Task 4–8 target where they require the fixed Router. The main checkout remains untouched; the current feature worktree stays isolated.
- `aa145e3ae9d02134741b310f2fdd8aa244c15927` was committed before this steering. RED exposed a missing native-success receipt binding and a real Windows nested junction that made outside bytes appear as verified output. GREEN binds operation/run/workspace, typed checks, workspace receipt and result bytes, and rejects reparse points during source export. Focused: 37 passed, 1 skipped, 28 subtests; Ruff passed. This applies to the old owner and is not evidence for the replacement architecture.
- The file-isolated affected run on `aa145e3` yielded 579 passed, 1 failed, 3 skipped across 25 files. The failure was the stdio `mcp_serve` EventBridge baseline test; it reproduced alone on that earlier revision. An immediate Windows `os.utime(None)` did not advance `st_mtime_ns`. A separate test-fixture change at `76a194dbe6420863b7b72c14d173645b7464b083` sets a deterministic later mtime; the focused case passed. The full affected set has not yet been rerun at that HEAD.
- `ebe90332c29800209737c044d8fb6386c4345694` records the retirement specification. `14c492734e01b4c59b591321d71c2b8bbf86275e` retires the old `hermes_start_engineering_run` from the new HTTP MCP surface and makes write capability false even when an old coordinator is injected. A real SDK negative test was RED before the change and GREEN after it. The replacement general task tool is not implemented, so write remains unavailable. The existing opt-in legacy plugin was not removed from main.
- On the retirement code, file-isolated `tests/control_mcp`: 12 files, 166 passed, 0 failed, 1 skipped; focused `tests/test_mcp_serve.py::TestEventBridgePollE2E::test_startup_baseline_suppresses_historical_replay`: 1 passed after the fixture repair. Ruff passed on changed files. These are component gates, not client acceptance or exact-head CI.
- `tools/delegate_tool.py` currently forwards a parent API key and credential pool to child AIAgents. It cannot satisfy the operator's tokenless worker rule as-is. A parent-brokered, contained subagent mode and real host approval remain required before any general MCP execution write is advertised. No endpoint, tunnel, account setting, client registration, PR or main merge was performed.

## 2026-09-24 recovered campaign baseline (T00; documentation only)

- Canonical recovered worktree: `C:/Users/downl/Documents/New project/hermes-control-mcp-recovered-20260923`, branch `feat/hermes-control-mcp-c-20260923-recovered`; start HEAD `76a194dbe6420863b7b72c14d173645b7464b083`, based on observed downstream `e6070028c9d0d75634ad7661c33fa937b682474a`. The two pre-existing dirty paths were this log and `RETIREMENT_SPEC.md`; no reset, stash, clean or replacement checkout was used.
- The `codegraph-3.zip` planning package is byte-identical to `codegraph-2.zip` (SHA-256 `7870af3fa739d541c19b0374f4ac6496e0b561ebf85848f21b5be9a0ad95b738`); its 28 manifest-listed members match their recorded hashes. These are planning inputs, not execution evidence. Frozen upstream release is `d337b736aa1e8ebecfab043842d13e4a2d2f48a3`; the distinct post-release ceiling is `b936546561888a54d5bf9cd7eae9629a824eb4f7`. Do not change the historical `b51c055a12220f8c7c18660e8599365012e19532` snapshot in `FEATURES.yaml` or `CARRY.yaml`.
- Approved CodeGraph CLI 1.6.0 was present. After an interrupted initial build, `sync .` completed on the recovered worktree: 8,898 indexed files, 196,154 nodes, 616,983 edges; status was up to date but retained an earlier-version re-index recommendation. The detached, fixed-ceiling comparison worktree on H: indexed 11,221 files, 238,177 nodes, 750,074 edges and reported up to date. Both indexes are local, ignored and not implementation/CI proof; owner/caller/callee evidence remains task-specific, and dynamic edges require source inspection.
- At the start HEAD, the focused Windows event-baseline test returned 1 passed in 7.79s; the same test on the separate downstream `main` checkout returned 1 passed in 12.72s. These runs do not reverse the recorded earlier 579/1/3 affected failure or establish a green full affected suite. RED/GREEN is not applicable to this documentation-only baseline; no new production fix is asserted.
- The native Windows restricted execution boundary, parent-owned inference, host consent, two-client MCP dogfood, independent review and exact-head CI have not been qualified. Write capability stays false. No push, PR, merge or deployment is authorized by this receipt.

## 2026-09-24 T07 provider-response semantics

- The H: isolated T07 worktree produced product commit `c5945fc05886f9663d882dd2e3c5f9fb8f02d8a6`, evidence commit `9b247239277c302f1969bbe4b16275ae08005f39`, and independent-review correction `8d7996d80d3894ef939596a45664438e903f50c0`. They were cherry-picked into this integrator as `2302039d1f`, `4adf4c0bc3`, and `91c5ac34d1`. The first meaningful RED was 9 failed and 3 passed in the new response-semantics file. After the correction, T07/T05 focused tests passed 27 in the H: worktree and 28 after integration; existing affected Codex Responses adapter/run-agent tests plus the new incomplete-terminal regression passed 81; the pending-tool-call file plus that regression passed 9. Ruff and diff checks passed. The initial H: CodeGraph before and after index receipts are under `evidence/codegraph/`; the post-correction sync used the global 1.5.0 CLI by mistake, so a pinned 1.6.0 after-index remains required for the corrected source.
- The independent security review initially blocked three cases: missing terminal SSE dispatch, request-model provenance, and unknown phase mismatch. Its second, read-only review approved integration after regression-backed fixes. This is an integration decision for the inactive controlled-inference seam. It does not enable MCP write admission; native isolation, host consent, real provider/client, exact final-head checks and downstream CI remain outstanding.

## 2026-09-24 T08 passive resource admission

- The isolated H: T08 worktree produced product/evidence/fix/evidence commits `1e1b9515f6ee8feedf3a6b754cc5cce22110709e`, `4fbc114500ccc607ceff5692936d41370b6830ed`, `3dfa896847760763cfa79af04f360f433c2a2198`, and `f761d9ffacc3a3f5e49118b56529c07fa2b33e55`. They were cherry-picked here as `73859f71b5`, `604f9d2760`, `349d94666e`, and `a7247f9de5`. Initial behavioral RED had six assertion failures; reservation reconciliation produced a separate assertion RED. Final focused resource and existing Windows contract tests passed 56 in H: and 56 after integration. Ruff, ty and diff checks passed. Pinned CodeGraph 1.6.0 after-sync reported 8,908 files, 190,728 nodes, 610,466 edges, no pending changes or refs and no worktree mismatch at product SHA `3dfa896847...`.
- The first independent review blocked unbounded GPU diagnostic stdout buffering. The correction limits streaming output to 16 KiB plus one overflow byte, terminates/reaps overflow and timeout children, and keeps diagnostics free of captured payloads. The second read-only review approved integration as an inactive work-in-progress seam. Trusted lifecycle reservation and per-process reflection producers are absent; T10 must bind reserve/start/reconcile/release/cancel/crash recovery before production admission. Real host pressure, GPU residency and calibration are unverified. No admission policy is active in the live host.

## 2026-09-24 T15 Security Center semantics and snapshot binding

- H: source/evidence commits `9869602a`, `f55fbeb7`, `95cf47df`, `1b98110c`, `db9bede6`, `1bca1c79` were cherry-picked as `8ba5d93b58`, `7d169aec25`, `92581fda17`, `e61d2419da`, `966b7bd733`, `d6a64857fe`. Two independent read-only reviewers approved the final snapshot correction at `db9bede6` after earlier P1 findings were fixed. The original T15 evidence documents behavioral RED for scan-time source swap/restore and uncapped public findings. Pinned CodeGraph 1.6.0 after-index at the final H: source reported 8,914 files, 191,022 nodes, 611,507 edges and no pending/mismatch.
- On integrated HEAD `d6a64857fe`, `tests/security/test_t15_security_semantics.py`, `test_security_center.py`, and `test_security_api.py` passed 73 with 2 real-detector EICAR cases deselected in 11.73 seconds. Four affected terminal tool files passed 26 in 8.05 seconds. The private snapshot is scanned and revalidated with the source before allowlist, recording or quarantine; public findings are bounded to 64. The tests do not prove real ClamAV/YARA detection, snapshot cleanup failure injection, or a kernel-enforced check-to-open guarantee. `SecurityService.status()` still invokes scanner version processes and must be corrected under T16 before read-only acceptance. No Desktop, Go, llama, endpoint, or live security configuration was changed.

## 2026-09-24 isolated-worktree retirement

- Before retirement, `f97835e34e` preserved 13 untracked T03/T04/T06/T07/T08 review and CodeGraph receipts in the integrator; four review documents had workstation-specific absolute paths replaced with neutral or repository-relative references. `062f79b349` separately preserved an ignored T04 strict-control type follow-up found during the deletion audit. The six T04 and two T07 CodeGraph receipts were byte-identical copies; review preservation was checked against the source after only the stated path substitutions.
- The integrated C: T00 event baselines, T01, T02, T03, T05, and T06 worktrees were removed earlier after ancestry or `git cherry` equivalence, status, ignored-data and absolute-path checks. The detached H: T09-before comparison was likewise removed. This continuation removed integrated H: T07, T15, T03, T08, and T06 worktrees after the same checks, with their registration and filesystem paths absent afterward; H: free space was about 27.90 GiB after the last removal. Active T10 and T16 worktrees remain. The C: T00 mtime worktree remains because it has two unique commits awaiting review.
- Integrated C: T04 was removed from Git registration with `git worktree remove --force` after preserving its untracked and ignored documentation; C: free space rose to about 6.31 GiB. Git left an unregistered residual directory containing `apps` and `node_modules` under that exact former worktree path. It was inspected but not manually recursively deleted because the operator specified `git worktree remove` as the removal method. These free-space values are point-in-time observations, not a durable capacity guarantee.
- The remaining C: T00 mtime worktree had two `git cherry`-unique documentation commits, but both changed only the same report, whose final Git blob `8ef9a4850e59dd69780441587a1a3a15460ef24e` was already integrated via `abfd0301a6`. The worktree was clean; its 108 ignored files were generated caches. After exact path and blob checks, `git worktree remove` removed its path and registration; C: free space was about 6.57 GiB. Two focused EventBridge fixture tests passed on the integrator, but WAL-only event polling without an explicit main-DB mtime update remains unverified.
- A further read-only check of the unregistered C: T04 residual found zero regular files and 3,878 reparse entries under `apps` and `node_modules`. The directory is therefore retained: recursive filesystem removal could cross link boundaries, and it would depart from the operator's Git-worktree-only removal instruction.

## 2026-09-24 T10 inactive delegation admission

- H: T10 commits `fbf81eae`, `458ca932`, `3a78f02e`, `23caf3d0`, `a16c5ab7` were cherry-picked as `5d87b37cda`, `91220858b1`, `31fa82f354`, `6165949f0a`, `20da65f8fd`. The first review found two P1 issues: independent owner instances could exceed the host cap using the same resource book, and token/clock exceptions after resource reservation leaked claims. Assertion RED reproduced both classes. The correction shares the synchronized ledger by `ResourceReservationBook` and places grant construction inside rollback; independent tests cover top-level, depth-two, UNKNOWN-tree and token/clock cases. A read-only reviewer approved the corrected source/test HEAD `23caf3d0` with the constraint that production wiring share one host resource book. Pinned CodeGraph 1.6.0 after-index on that HEAD reported 8,918 files, 191,138 nodes, 612,124 edges, no pending references or worktree mismatch.
- On integrated HEAD `20da65f8fd`, nested-budget, round-robin, resource-admission and free-route-catalogue tests passed 63 in 2.88 seconds. Ruff and ty both passed on the T10 source and focused tests. T10 is still an inactive admission owner: no actual child launcher, lifecycle reconciliation, deadline enforcement, persistent grant recovery, or two-task live completion was verified. An injected clock regression is not rejected, although production uses `time.monotonic`.
- After integration, the T10 H: branch had zero `git cherry`-unique commits, six untracked `.npm-cache` generated files, 1,306 ignored generated files, and no reparse entries. The absolute path and status were checked before `git worktree remove --force`; path and registration were absent afterward, with H: free space about 29.28 GiB. Active T16 and new T09 continuation worktrees are retained.

## 2026-09-24 T12 host native receipt evaluator component

- The isolated T12 worktree committed `6a11de0e46d45d9d30ad724dc550a8d7dd4dfb83` and it was cherry-picked into the integrator as `3bf70a018b`. An independent read-only review found no blocking issue within the stated partial evaluator scope. The review explicitly noted that the after CodeGraph receipt names predecessor SHA `9b6bdfe6...` and binds the staged product tree with its separate fingerprint; it must not be cited as a committed-product-SHA index.
- Importable-stub RED was 21 assertion failures and 1 pass, then 24 focused GREEN. At integrated HEAD `3bf70a018b`, native receipt, provenance and read tests passed 62 with 1 Windows symlink-privilege skip. Ruff, ty, staged diff check and receipt-schema checks passed in the isolated worktree. The component binds host expectation to owner epoch, operation/run/profile/workspace, source/result digests, protected check-set revision, platform and actual process/check exit, timeout, completion and cleanup facts. It has no production caller. A trusted native producer, durable journal/fenced writer, real T06 Windows execution, separate approved apply and live MCP write gate remain unqualified.
- Before removal, `git cherry` marked the T12 commit equivalent in the integrator. The absolute H: target was under the intended worktree parent and its root was not a reparse point; tracked status was clean. The only 22 untracked files and 1,138 of 1,144 ignored files were task-local `.npm-cache` content, with four Ruff cache and two CodeGraph entries. No reparse entries or unsaved review/user work were found. `git worktree remove --force` removed only that audited worktree; the path and Git registration were absent afterward, and H: free space was about 23.88 GiB.

## 2026-09-24 T11 delegated network budget component

- Isolated H: source/evidence/correction/evidence commits `cdfd150803`, `3a80f28969`, `97cbe8f004`, `6ad5f214f9` were cherry-picked into the integrator as `154897107f`, `a859841418`, `9f7f5d4821`, `39f175116c`. The independent reviewer first blocked a cancellation race: a later deadline could replace the first user-cancel reason and call the transport abort callback twice. Meaningful RED reproduced both repeated budget cancellation and external interrupt followed by deadline; the corrected first-cancel guard passed independent re-review.
- On integrated HEAD `39f175116c`, the seven affected Python test files passed 63 in 128.95 seconds; changed-file Ruff and `git diff --check HEAD~4 HEAD` passed. Pinned CodeGraph 1.6.0 at source correction SHA `97cbe8f004...` reported 8,922 files, 191,367 nodes, 612,964 edges, zero pending changes/references and no mismatch. Earlier before/after receipts and exact commands are in `evidence/T11-network-budget-20260924.md`.
- The budget remains process-local and inactive: no production host supplies both the trusted account scope and budget to the parent inference port. Cross-process limits, real provider abort, DNS, TLS/redirect, external Retry-After and actual MCP write remain unqualified. A non-terminating provider worker holds its account capacity rather than treating an unknown outcome as free. This integration does not change endpoint or client configuration.
- Retirement audit after integration: `git cherry HEAD codex/t11-network-budget-20260924` marked all four source/evidence commits equivalent. The exact absolute target was `H:\hermes-control-mcp-t11-network-budget-20260924`, rooted on H: and not a reparse point; its tracked/untracked status was clean, and a recursive reparse inventory found none. Ignored content consisted of local CodeGraph, pytest/Ruff, Python bytecode and Node compile caches; the `.tmp-codegraph` files were Node compile-cache entries. All T11 review and graph receipts were committed, with no other user work found. `git worktree remove --force` removed only this audited target; path and Git registration were absent afterward, and H: free space rose from about 26.53 to 28.02 GiB.

## 2026-09-25 operator continuation: preserve main and integrate T09 profile scope

- The operator selected `b1d48ec202f7e3170ab3f2ff520056443ca65ce7` for integration and directed all further work to `main`. It is an ancestor of main through merge commit `122e4ac3e223b83f5684837c4159d73fe8d56f86`. The merge introduced zero tracked deletions; no tracked deletions were found from that merge through T09 source commit `e8ede825452ed723baa68a5460fb14da638b0ec2`. Existing source worktrees and local evidence remain preserved.
- T09 profile-isolation RED: the host started under profile A with explicit opt-in; after A's opt-in was revoked and ambient `HERMES_HOME` switched to opted-in profile B, the pre-fix refresh consulted the ambient profile and fetched for A. GREEN: the host now captures A's resolved home and its per-fetch opt-in callback loads config under an existing context-local Hermes-home override that is reset in `finally`; no process-global environment mutation was introduced. The main fix is source commit `e8ede825452ed723baa68a5460fb14da638b0ec2`.
- Independent read-only review approved the profile pinning. It confirmed the config cache keys by profile path and identified no cross-profile authorization leak. Residual: the generic config loader retains same-profile last-known-good settings on malformed YAML, so this change does not guarantee immediate opt-out when that profile's file becomes invalid.
- Direct pytest for `tests/agent/test_free_route_catalogue.py` passed 45 tests. After adding integration coverage, the repository file-isolated runner `& '.\.venv\Scripts\python.exe' scripts/run_tests_parallel.py -j 2 tests/agent/test_free_route_catalogue.py tests/agent/test_free_route_catalogue_t11_integration.py tests/control_mcp/test_catalogue_host_lifespan_integration.py tests/gateway/test_free_route_catalogue_lifecycle_integration.py -q` passed 51 tests in 4 files, with 0 failures. `uvx --from ruff==0.15.10 ruff check` passed for the two source and four test files.
- The additional T11 tests exercise the real bounded DNS worker, request lease, and profile cache lock; the Control MCP and Gateway integration tests exercise their existing lifecycle owners with fake network adapters. They do not establish live HTTP/provider, actual MCP request routing, packaged Desktop behavior, or production Gateway adapter health. The integration tests themselves did not produce a product-behavior RED; their first setup failures were corrected without being counted as behavioral regressions.
- One exploratory `run_tests_parallel.py --help` invocation unexpectedly began the full suite. It was interrupted around 553 displayed tests with no failure displayed; this is not full-suite evidence. Full suite, exact-final-head CI, live client read/write, and security acceptance remain open. No push or endpoint/client configuration change was made during this continuation.

## 2026-09-25 T16 bounded Security Center semantics on main

- The requested worktree SHA `b1d48ec202f7e3170ab3f2ff520056443ca65ce7` is an ancestor of local `main` through merge commit `122e4ac3e223b83f5684837c4159d73fe8d56f86`; this continuation preserved all files and worktrees. The main-branch T16 semantic port is product commit `e310ad317f2b4a7ec651207c62f1101ad8e48372`, based on `1326ee6dc9c8eb8a9d92be0a1ef0b5704c1a41e4`. It adds bounded process execution, walking, definition inventory and snapshot reservations, and hardens engine revision binding, cache validation, source-path reparse checks, watcher limits and read-only status behavior.
- Regression RED for the six independent review findings was `6 failed, 2 skipped, 45 deselected`; synthetic ancestor-reparse swap was separately `2 failed`. After fixes, the focused cases were `9 passed, 2 skipped, 44 deselected`; the T16 file was `51 passed, 4 skipped`. On exact product HEAD `e310ad317f2b4a7ec651207c62f1101ad8e48372`, the six related files (`test_t16_bounded_security.py`, `test_security_center.py`, `test_security_auto_update.py`, `test_watch_state.py`, `test_t15_security_semantics.py`, `test_security_api.py`) passed `157` with `5` skips in `29.24s`. Pinned Ruff 0.15.10 and `git diff --check` passed.
- Independent read-only code review approved the corrected six findings. A second gate review approved a local commit only when explicitly marked `NATIVE_UNQUALIFIED`; it rejected push, release, and native qualification claims. Its review receipt is `.omo/evidence/t16-semantic-port-gate-review.md`. The requested SHA remains in `main` history, and T16 added no tracked-file deletions.
- LM21/LM22/LM24 have code and inert-fixture evidence for this local port. LM23 stays `NATIVE_UNQUALIFIED`: there is no managed ClamAV definition database on this workstation, so real benign and EICAR outcomes were not tested. Other skips cover a POSIX-only process-group case and three Windows directory-symlink setup limitations; synthetic reparse-attribute checks for both hash and snapshot boundaries passed. A privileged real junction race, fixed CodeGraph 1.6.0 after-index, campaign-wide exact-final-head CI, live-client, native-runtime and remaining security-closeout gates are open. The shared snapshot budget is process-local, not cross-process. No push, scanner database setup, runtime configuration change, endpoint exposure or worktree removal was performed.
