# Receipt — SR-007b (2026-09-13)

## Preconditions

- Pre-slice HEAD: `c878de51852290e7352fd5183f008674e2a1a7d7`
- Frozen U: `6dd091a89c33e6e4909a80f78343bb384deb5ca8`
- Card chrome / guided film: SKIP

## Contract

Credential-miss submit (deferred warning or provider-setup error) must open a **visible** recovery UI on the existing Desktop onboarding overlay:

- First-run (`configured !== true` and not skipped): blocking `requested` path unchanged.
- Past first-run (`configured === true` or `firstRunSkipped`): route through `startManualOnboarding` (Settings → Providers equivalent) so the overlay is not gated away by `configured && !manual`.

## Implementation

- `apps/desktop/src/store/onboarding.ts::requestDesktopOnboarding`
- Tests in `apps/desktop/src/store/onboarding.test.ts`

## Tests

```text
npx vitest run src/store/onboarding.test.ts
→ 19 passed (includes 3 SR-007b cases)
```

## Status flags

| Flag | Value |
|---|---|
| PASS_FOCUSED | yes |
| LOCAL_DEPLOYED | NOT_RUN |
| soak | NOT_RUN |

## Tested SHA

- `origin/main` / HEAD: `91b4e09e98525c33f35d16e2281337a50dd56383`
