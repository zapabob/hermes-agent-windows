# GPT-6-pro handoff: Windows-native v0.21.4 semantic adoption

Snapshot: 2026-09-24 JST. This is a status report and executable review plan. It does not amend the approved frozen upstream ceiling, declare release readiness, or authorise a production deployment.

## Exact repository and workspace state

- Only target repository: `zapabob/hermes-agent-windows`.
- Integration worktree: `C:\Users\downl\Documents\New project\hermes-control-mcp-recovered-20260923`; branch `feat/hermes-control-mcp-c-20260923-recovered`; snapshot HEAD `4e4cfe24e83f109332f3e168fd6613c012b1ada0`. The branch is active and may advance after this report.
- Original user checkout: `C:\Users\downl\Documents\New project\hermes-agent`, `main` at `e6070028c9d0d75634ad7661c33fa937b682474a`; leave it untouched. The integration worktree has unrelated untracked `.t06-pytest-integrator-safe-20260924-01/`; preserve it.
- Frozen upstream inputs: retained historical B `b51c055a12220f8c7c18660e8599365012e19532`, 0.21.3 R0 `345cd2b057a452236de401d3534b8502a7465e8d`, formal 0.21.4 R1 `d337b736aa1e8ebecfab043842d13e4a2d2f48a3` (`v2026.9.21`), and approved post-release ceiling U `b936546561888a54d5bf9cd7eae9629a824eb4f7`. All four commit objects were locally present when checked. The 0.21.4 adoption worktree remains untouched.
- A read-only `git ls-remote` on 2026-09-24 observed newer upstream `main` at `94119759c69bdbef43efe229d079ecebedf69106`. This is drift beyond U, not an input to the current frozen campaign. A separately reviewed scope amendment must identify any urgent post-U security fix before implementation.
- An official GitHub compare API read from U to that observed `main` reported `ahead_by=727`, `behind_by=0`. Its first response exposed 250 commit records; this is not a complete 727-commit semantic inventory. Candidate families visible in that partial set include Desktop replay/resume ordering (`bb7788c62660`, `1a0b6ff786ae`), Windows absolute-path handling (`d78355cf5986`), configuration migration (`a88bef98b279`, `49aaa52d33d6`), and credential refresh/stale-writer behavior (`3c424f5b2b23`, `d2f54de2cc5b`, `badd8f56e449`). These are commit-title leads only. Review final code and regressions before deciding whether any post-U security correction justifies an explicit amendment. Compare: https://github.com/NousResearch/hermes-agent/compare/b936546561888a54d5bf9cd7eae9629a824eb4f7...94119759c69bdbef43efe229d079ecebedf69106.

## Evidence-backed progress at this snapshot

- T01 enumerated commit metadata in three fixed windows: B..R0 2,777; R0..R1 5,173; R1..U 993; unique window commits 8,943. Together with 5,105 historical ledger rows, `metadata_universe.jsonl` has 14,048 rows. These are metadata, not code review. `adoption-decisions.jsonl` still has 34 seed-only `UNREVIEWED` rows, including 16 P0 seeds. The freeze records 1,671 critical historical rows without a current semantic receipt. No complete upstream-parity percentage is established.
- Selective legacy actor/Docker Router retirement, default-disabled MCP host seam, scoped reads, approval and parent-inference components, provider response semantics, passive resource admission, static free-route classification, inactive bounded nested admission, and T15 security semantics have reviewed partial commits on the integration branch. None proves live general MCP write readiness. The log in `docs/control-mcp/IMPLEMENTATION_LOG.md` records exact prior SHAs and gates.
- T12 host native receipt evaluator was integrated as `3bf70a018b` and its worktree retired; relevant integrated tests: 62 passed, 1 skipped. It has no production caller. T06 Windows AppContainer positive/negative qualification and one-time profile setup approval remain pending; receipt production, journal fencing and separately approved apply therefore remain open.
- T09 actual OpenRouter catalogue refresh and T11 network budget are active in isolated H: worktrees. T16 bounded Security Center source/evidence commits are `026b550cef6c03a85b77236457531f9ac7c59747` and `afe3854002b329b75cf586b607014cda187cfac2`; independent integration review is pending. T16 focused run passed 119 with two environmental skips; an additional affected security API file passed 12. The installed ClamAV returned exit 2 for benign and EICAR fixtures, so live detection is not qualified.
- No exact final-HEAD full gate, real two-client approved write, PR, main merge, post-merge CI or deployed-client acceptance is claimed. No operating endpoint, tunnel, Desktop/Go/llama process, or original checkout was changed by this report.

## Semantic adoption plan for 0.21.4

1. **Keep one source universe.** Use `freeze.json`, `inventory/*.jsonl`, the legacy ledger, `ADOPTION_SEEDS.json`, and the approved B→R0→R1→U commit ranges. Prove ancestry and full history. Keep merge-resolution commits and group salvage, fixup and revert families; do not infer behavior from commit title, file name or patch-id alone. Record newer `94119759...` only as drift unless an explicit frozen scope amendment is approved.
   To address the requested latest-upstream view, complete a separate U→observed-main drift inventory across all 727 commits, compare final source and security advisories against current downstream contracts, and write named amendment candidates with an exact new ceiling, owner, Windows acceptance test, and rollback. Do not silently merge that inventory into the approved R1/U adoption ledger.
2. **Build family cards from actual code.** For every reviewed family, record exact upstream commits and final behavior at U, current downstream owner/call path, Windows-specific constraints, decision (`ADOPT`, `COMPOSE`, `ALREADY_EQUIVALENT`, `KEEP_DOWNSTREAM_STRONGER`, `NOT_APPLICABLE`, `DEFER`, or `SUPERSEDED_OR_REVERTED`), authorship, RED/GREEN, CodeGraph D/U receipts and native proof. Metadata rows remain `UNREVIEWED` until this is done. Reuse the existing owner and feature ledgers to prevent parallel file conflicts.
3. **T17, P0 first.** Review provider secret/profile propagation, response/tool-call completion, Desktop single-backend ownership, state.db WAL/reader-writer behavior, MCP discovery and approval replay, plugin trust, native process cleanup, updater locked-file recovery, security finding precedence and no-secret cross-origin retries. Port the smallest observable contract into the existing Windows owner. Keep the Go watchdog limited to embedding llama, Electron as the Desktop backend owner, and the existing Git/approval authorities. Do not import Linux service supervision as a Windows default.
4. **T18, useful feature families next.** Review structured CLI output, skill auto-load with prompt-cache stability, temporal session search, background result delivery, model picker/effort clarity, plugin lifecycle and approved local-engine update. For each feature, first prove whether the recovered branch already supplies equivalent behavior. Preserve local llama/GGUF/hot-swap, embeddings, Ebbinghaus/Semantic Graph, voice/VRChat/Unity/AITuber and Switchyard/MoA/fallback as independent downstream contracts. Classify macOS, Linux and hosted-only portions with evidence and a revisit trigger rather than copying their default startup paths.
5. **Finish the native control dependency chain.** Complete T06 useful restricted Windows execution and credential-denial tests, then T12 trusted receipt producer/fenced writer/verified apply. Finish T09/T11 and T13/T14 memory handoff, T19 authoritative Desktop state, and T20 real scoped MCP operations. Test one genuine human-once approval, two scoped clients, denial/replay/cancellation and mutation. Do not advertise C writes from an inactive seam or a fixture-only verdict.
6. **Qualify and decide the version at T24.** Require zero unreviewed or unproved P0 families, exact final-feature-HEAD Python/parity, Ruff/typing, Desktop JS/TS/lint/i18n, native Windows/Go, security and dependency-lock checks, mutation tests, independent security review, and local-client evidence. Preserve the historical `FEATURES.yaml`/`CARRY.yaml` snapshot SHA. Record upstream compatibility as R1 plus U separately from the downstream product version. Only then decide whether the public contracts permit a 0.21.3→0.21.4 patch bump; inspect `pyproject.toml`, `hermes_cli/__init__.py`, `apps/desktop/package.json`, `downstream/distribution.json` and related lock/build metadata together. An incompatible public change needs an explicit version decision.
7. **Publish through T25 only after those gates.** Use one reviewed downstream PR, exact feature-HEAD required checks and security review, normal protected merge, and separate post-merge-main CI. Runtime rebuild/restart and installed Desktop/ChatGPT/Codex client tests are separate authorised deployment evidence. No direct main push, force push, false `ci-reviewed`, or cleanup of user work.

## Requested GPT-6-pro review

Review this report and the frozen package `C:\Users\downl\Downloads\hermes-workstation-master-plan-20260924-codegraph.zip` against the actual integration HEAD. Identify the highest-risk P0 semantic families and any contradictory assumptions, propose a family-by-family execution order with disjoint file ownership and exact acceptance tests, and state which current rows are only metadata. Check whether a post-U urgent-security amendment is justified by a named, verified fix; do not silently expand U. Return a revised plan and blocker list to the integrator. Implementation already authorised under the existing campaign should continue in parallel on its isolated worktrees.
