# Windows runtime authority: G1/G2

Audit base: `18850122ed29b41ed24c526c63289cd2c9fc437f`.
Runtime scope is limited to embedding-child environment and operator watchdog process identity. No upstream tree mirroring, Desktop ownership migration, inference-model changes, or deployment restart is included.

## Before / after

| Boundary | Before | After |
| --- | --- | --- |
| Watchdog -> embedding llama-server | Parent environment except incompatible cache type | Child-local credential-name filter; parent is not mutated; OS/GPU/inference settings and this server's own `LLAMA_API_KEY`, API-key file and TLS-key file remain usable |
| Denied identity query | Visible Session 0 PID plus serialized lock path/root could become owned | Query failure remains unverified, preserving the lock; only proved absence/exit is stale |
| Operator Session 0 displacement | Failed handle open could fall back to a numeric-PID stop | Bounded retry using the operator's existing SeDebugPrivilege, restored to its prior state; live creation time/image must still match on the exact termination handle |
| Stop completion | Wait result discarded | Non-signalled/failed wait preserves evidence and reports failure |
| Privilege API errors | Error 87 from a token/privilege API could be mistaken for missing PID | Only process-open/exit evidence establishes absence; helper API failures remain unverified |

Electron remains the default backend owner. The Go Session 0 prohibition on killing interactive Desktop is unchanged. No mutation API is added. G3-G6 are not part of this change.

## Regression-first evidence

G1 test commit: `63b9a14b1b59ae65cad96d135e3574054850a7d2`.
Windows Tier-1 run `35121028627`, job `104878548468`, ran `go test ./...` against the PR merge tree. `TestEmbeddingDoesNotInheritUnrelatedCredentials` failed on 16 synthetic credential names; the local-inference preservation test did not fail. The subsequent G1 production change is `140367cee23a9daaef2e40c671f2dc4b8a02422f`.

G2 regression commit: `5e408e3f69ebe3bf938118570e439c941274a20d`. The tests load production PowerShell AST function definitions, never execute the launcher, and use temporary locks. Denied query, invisible/denied process, denied terminate handle, incarnation change, image change, verified Session 0 identity, and incomplete wait are tested with non-destructive native-call doubles. A separate real-child test validates and terminates only its retained harmless sleeper. Both Windows PowerShell 5.1 and PowerShell 7 are required on Windows.

### Native results captured in run 35126832848

Branch revision: `7f50fcacedbb5d38e093fe99c2811f4e459bf5dd`.
Tested PR merge tree: `9595210e8354c277954387810dcab22f6b574c8b`.
Artifact: `windows-runtime-authority-35126832848` (ID `10460395258`).

- Original G2 baseline: seven failures at the unsafe authority decisions; one separately identified legacy PowerShell 5.1 `0UL` syntax failure. All eight are asserted and recorded, but the syntax failure is not claimed as an ownership counterexample.
- Privilege-provenance baseline: production launcher from `bceee3c62ff617122349661a5df069279113c564`, with the corrected compiler-compatible test double. Eight specific assertion failures, ten passing controls, no skips or collection errors. The production file is checked as unmodified and the fixture hash is archived.
- Current native authority and lifecycle suites: **52 passed, 1 skipped**. The skip is the existing non-elevated-launch test on the elevated CI runner; the new process/privilege cases run on both Windows shells.
- Full watchdog `go test -count=1 ./...`: passed. `go vet ./...` is a separate required workflow step.
- Carry reports were regenerated by the existing script against the committed tested tree, not manually estimated. Its existing `_docs/` exclusion makes this documentation/report-only commit non-self-invalidating.

## Separate validation repairs

The existing clean-checkout Desktop typecheck attempted to consume unbuilt referenced declaration outputs (TS6305). A source-only checking config inherits all compiler options, include/exclude rules and path aliases, while leaving the production reference graph intact. Renderer, Electron and E2E still each run a no-emit typecheck. No dependency or runtime behavior was changed.

The maintainer's GitHub commit email is recorded using the existing contributor mapping format. No attribution gate was removed.

## Validation commands

```powershell
python -m pytest tests/test_go_watchdog_runtime_authority.py tests/test_go_watchdog_privilege_authority.py tests/test_go_watchdog_launcher_authority.py tests/test_go_watchdog_architecture_contract.py tests/test_go_watchdog_task_registration.py -q
Push-Location scripts/windows/watchdog-go
try { go test ./...; if ($LASTEXITCODE) { exit $LASTEXITCODE }; go vet ./... } finally { Pop-Location }
```

Passing these focused tests is not a claim that repository-wide CI is green. Required checks must complete on the final PR revision and again on the merged main revision. No test or gate is disabled by this patch.

## Security limits

Environment-key filtering reduces accidental propagation; it is not an OS sandbox and does not prevent a same-user process reading files. Explicit llama-server inbound authentication is not a provider credential. No secret values are logged by the new tests or policy.

SeDebugPrivilege is not newly granted: Windows can only enable a privilege already present in the operator token. Failure to enable, restore, query, validate, terminate, or wait does not authorize a PID fallback. Operator PowerShell sessions carrying the old in-memory Add-Type definition must be restarted rather than silently using the previous policy.

The native tests use real Windows process handles for a retained harmless child, and doubles for Session 0/privilege denial scenarios. They do not claim an actual S4U boot/logon soak test or deployment to the operator's running workstation.
